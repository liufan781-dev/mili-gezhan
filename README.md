# AI Trainer Portfolio V2

这是一个全新、独立的 React + Vite 个人作品集基础版本。

## 运行

```bash
npm install
npm run dev
```

## 替换内容

- 简历信息：编辑 `src/data.js`
- Hero 视频：将视频命名为 `hero-video.mp4` 并放到 `public` 目录
- 当前人物图与项目图均为 CSS 视觉占位，不包含旧网站或生成图片素材
